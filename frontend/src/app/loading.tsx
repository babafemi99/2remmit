import { PageLoader } from "@/components/loading/page-loader";

export default function RootLoading() {
  return (
    <PageLoader
      label="Loading 2Remit…"
      description="Preparing your payout console."
    />
  );
}
